name: Copilot runner

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  copilot:
    runs-on: [self-hosted, macos]
    steps:
      - uses: actions/checkout@v4
      - run: echo "Runner is available for Copilot"